spring:
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 587
    username:
    password:
    protocol: smtp
    properties:
      mail:
        smtp:
          #socketFactory:
            #port: 465
            #class: javax.net.ssl.SSLSocketFactory
            #fallback: false
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
  thymeleaf:
    enable: true

envio:
  email:
    remetente: 'noreply@teste.com'
    nomeRemetente: 'Usuário Teste'

server:
  port: 8082